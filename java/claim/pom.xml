<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>claim</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>claim</name>
    <description>Claim processing service</description>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.18</version>
        <relativePath/>
    </parent>
    <properties>
        <java.version>11</java.version>
        <!-- Security overrides -->
        <tomcat.version>9.0.118</tomcat.version>
        <logback.version>1.2.13</logback.version>
        <!-- CVE-2024-38821, CVE-2025-22228: spring-security → 5.8.16 (latest 5.8.x) -->
        <spring-security.version>5.8.16</spring-security.version>
        <!-- CVE-2024-22243/22259/22262, CVE-2025-41249: spring-framework → 5.3.39 (latest 5.3.x) -->
        <spring-framework.version>5.3.39</spring-framework.version>
        <!-- CVE-2022-1471: snakeyaml → 2.0 -->
        <snakeyaml.version>2.0</snakeyaml.version>
        <!-- CVE-2023-52428: nimbus-jose-jwt → 9.37.2 (also bumps json-smart transitively) -->
        <nimbus-jose-jwt.version>9.37.2</nimbus-jose-jwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- This module's <parent> is spring-boot-starter-parent, not the root
                 java/pom.xml, so root's dependencyManagement does NOT apply here —
                 postgresql/jackson must be pinned locally or they silently fall back
                 to Spring Boot 2.7.18's own (older, vulnerable) BOM defaults. -->
            <!-- CVE-2026-54291: postgresql → 42.7.12 -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>42.7.12</version>
            </dependency>
            <!-- CVE-2025-52999, GHSA-r7wm-3cxj-wff9, CVE-2026-54512/54513/59888:
                 jackson-core → 2.18.8 -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>2.18.8</version>
            </dependency>
            <!-- CVE-2026-59889, GHSA-mhm7-754m-9p8w: jackson-databind → 2.18.9,
                 one patch ahead of jackson-core/-annotations -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>2.18.9</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>2.18.8</version>
            </dependency>
            <!-- spring-security versions managed by <spring-security.version>5.8.16</spring-security.version> property -->
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- jpa, crud repository -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- PostgreSQL -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.microsoft.sqlserver</groupId>
            <artifactId>mssql-jdbc</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>2.12.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.sunbirdrc</groupId>
            <artifactId>pojos</artifactId>
            <version>2.0.3</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.jayway.jsonpath</groupId>
            <artifactId>json-path</artifactId>
            <version>2.7.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.sunbirdrc</groupId>
            <artifactId>middleware-commons</artifactId>
            <version>2.0.3</version>
            <scope>compile</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
